## 2024-05-24 - Navigation Accessibility and Consistency
**Learning:** Global navigation elements replicated across multiple HTML files require synchronized updates for ARIA attributes and relative paths to maintain a seamless and accessible user experience.
**Action:** Use a consistent pattern for icon-only interactive elements (`aria-label` on the anchor, `aria-hidden` on the icon) and ensure relative paths are correctly mapped based on the file's location.
